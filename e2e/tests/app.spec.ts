import { expect, test } from "@playwright/test";

// Placeholder e2e test. Launching the built Electron app and asserting the
// window, sidebar, and tab-creation flow lands in a later milestone (m5).
test("placeholder", () => {
  expect(true).toBe(true);
});
